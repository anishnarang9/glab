// P2 — /team route. SSR list of researchers with current projects + latest matched papers.
export default function TeamPage() {
  return <main className="min-h-screen p-8 text-indigo-950">Team view coming soon.</main>;
}
