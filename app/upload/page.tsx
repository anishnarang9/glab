// P2 — /upload route. Form: type + content + share checkbox. Calls /api/upload.
export default function UploadPage() {
  return <main className="min-h-screen p-8 text-indigo-950">Upload coming soon.</main>;
}
